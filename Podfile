# Uncomment the next line to define a global platform for your project
platform :ios, '14.0'
use_frameworks!

target 'UsersMap' do
  pod 'Mapbox-iOS-SDK', '~> 6.3.0'
  pod 'SDWebImage', '~> 5.0'

  target 'UsersMapTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
